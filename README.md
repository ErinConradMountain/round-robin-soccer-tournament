# round-robin-soccer-tournament
testing a website that takes the scores of soccer teams for our Six-A-Side tournament

## Running a Local Server

To preview the site locally, start a simple server in this folder:

```bash
npx http-server
# or
python -m http.server
```

Open <http://localhost:8080> in your browser to view the site.
